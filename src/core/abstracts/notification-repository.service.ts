/*
import { Notification } from "../entities";

export abstract class INotificationServices {
    abstract searchNotification(options: any, whereEqualsObj?: any, whereLikeObj?: any): Promise<Notification[]>;
}
*/